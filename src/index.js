import "./styles.css"
import { createNavBar } from "../modules/header";
import { createTask } from "../modules/task";

const content = document.getElementById("content");

//createNavBar(content);
createTask(content);


